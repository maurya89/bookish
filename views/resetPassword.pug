extends layout

block content
  form()


    -console.log(token);
    input(type="hidden" value=token id= 'token')
    legend Reset Password
    .form-group
      label(for='username') Password
      input.form-control(type='password' id= 'password',, name='username', autofocus, required)
    .form-group
      label(for='password') Confirm Password
      input.form-control(type='password', id= 'confirm-password' name='password', required)
    button.btn.btn-primary(type='button' id = 'resetPassword') Submit
    